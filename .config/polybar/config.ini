[section/base]
include-file = ~/.config/polybar/modules.ini
include-file = ~/.config/polybar/user_modules.ini
include-file = ~/.config/polybar/colors.ini

[global/wm]
margin-bottom = 0
margin-top = 0

[bar/main]
; This bar will have its properties inherited by other bars.

; Dimension defined as pixel value (e.g. 35) or percentage (e.g. 50%)
width = 100%
height = 30

; Offset defined as pixel value (e.g. 35) or percentage (e.g. 50%)
; the percentage can optionally be extended with a pixel offset like so:
; 50%:-10, this will result in an offset in the x or y direction 
; of 50% minus 10 pixels
; offset-x = 0.5%
; offset-y = 0.5%

;  Tell the Window Manager not to configure the window.
; Use this to detach the bar if your WM is locking its size/position.
; Note: With this most WMs will no longer reserve space for 
; the bar and it will overlap other windows. You need to configure
; your WM to add a gap where the bar will be placed.
override-redirect = false
wm-restack = bspwm

; Put the bar at the bottom of the screen
bottom = false

; Background ARGB color
background = ${dracula.background}
; Foreground ARGB color
foreground = ${dracula.foreground}

; Values applied to all borders
; Individual side values can be defined using:
;   border-{left,top,right,bottom}-size
;   border-{left,top,right,bottom}-color
border-size = 1
border-color = ${dracula.comment}

; Enable support for inter-process messaging
; See the Messaging wiki page for more details.
enable-ipc = true

; Requires polybar to be built with xcursor support (xcb-util-cursor)
; Possible values are:
; - default   : The default pointer as before, can also be an empty string (default)
; - pointer   : Typically in the form of a hand
; - ns-resize : Up and down arrows, can be used to indicate scrolling
cursor-click = pointer
cursor-scroll = ns-resize

; Number of spaces to add at the beginning/end of the bar
; Individual side values can be defined using:
;   padding-{left,right}
padding-left = 0
padding-right = 1

; Number of spaces to add before/after each module
; Individual side values can be defined using:
;   module-margin-{left,right}
module-margin = 1

; Fonts are defined using <font-name>;<vertical-offset>
; Font 1. Text font
font-0 = Fantasque Sans Mono:style=Regular:pixelsize=12;3

; Font 2. Icon font.
font-1 = FiraCode Nerd Font:style=Regular:pixelsize=15;3

; Font 3. Text font for asian characters.
font-2 = Noto Sans Mono CJK JP:style=Regular:pixelsize=12;3

# Modules
modules-left = bspwm cpu memory spotify
modules-center = 
modules-right = redshift pacman-updates backlight pulseaudio battery network date time 

# System tray
tray-position = right
tray-padding = 2
tray-maxsize = 16
